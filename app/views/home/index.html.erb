<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BaseCamp</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <style>
    /* Reset default margins and ensure full height */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body, html {
      height: 100%;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.6;
    }

    /* Hero Section */
    .hero {
      position: relative;
      width: 100vw; /* Full viewport width */
      height: 100vh; /* Full viewport height */
      background-image: url('https://images.unsplash.com/photo-1521737604893-d14cc237f11d');
      background-size: cover;
      background-position: center;
      background-attachment: fixed; /* Parallax effect */
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      text-align: center;
      overflow: hidden;
    }

    .overlay {
      position: absolute;
      inset: 0; /* Shorthand for top, right, bottom, left */
      background-color: rgba(0, 0, 0, 0.6); /* Slightly darker for better contrast */
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 90%; /* More flexible for smaller screens */
      padding: 2rem;
    }

    .hero-content h1 {
      font-size: clamp(2rem, 5vw, 3.5rem); /* Responsive font size */
      font-weight: 700;
      margin-bottom: 1.5rem;
      line-height: 1.2;
    }

    .hero-content p {
      font-size: clamp(1rem, 2.5vw, 1.25rem);
      margin-bottom: 2rem;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    /* Button Styles */
    .btn {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      font-size: 1rem;
      font-weight: 500;
      border-radius: 0.375rem;
      text-decoration: none;
      transition: background-color 0.3s ease, transform 0.2s ease;
      cursor: pointer;
    }

    .btn-primary {
      background-color: #007bff;
      color: #fff;
    }

    .btn-primary:hover, .btn-primary:focus {
      background-color: #0056b3;
      transform: translateY(-2px);
    }

    .btn-success {
      background-color: #28a745;
      color: #fff;
    }

    .btn-success:hover, .btn-success:focus {
      background-color: #1e7e34;
      transform: translateY(-2px);
    }

    /* Accessibility Improvements */
    .btn:focus {
      outline: 2px solid #fff;
      outline-offset: 2px;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .hero-content h1 {
        font-size: clamp(1.5rem, 4vw, 2.5rem);
      }

      .hero-content p {
        font-size: clamp(0.875rem, 2vw, 1rem);
      }

      .btn {
        padding: 0.6rem 1.2rem;
        font-size: 0.9rem;
      }
    }

    @media (max-width: 480px) {
      .hero-content {
        padding: 1rem;
      }

      .hero {
        background-attachment: scroll; /* Disable parallax on small screens */
      }
    }
  </style>
</head>

<body>
  <!-- Hero Section -->
  <section class="hero" aria-label="Welcome to BaseCamp">
    <div class="overlay"></div>
    <div class="hero-content">
      <h1>Welcome to BaseCamp: Organize, Collaborate, Succeed</h1>
      <p>A simple and efficient way to manage your projects, collaborate with your team, and stay organized.</p>
      <% if user_signed_in? %>
        <a href="<%= projects_path %>" class="btn btn-primary">Go to Your Projects</a>
      <% else %>
        <a href="<%= new_user_registration_path %>" class="btn btn-success">Get Started</a>
      <% end %>
    </div>
  </section>
</body>
</html>